# generic plugins
plugins=(git composer common-aliases yii2 bgnotify z extract yii2)

# os-specific plugins

case $(uname) in
	"Darwin")
		plugins=("${plugins[@]}" "brew" "brew-cask" "osx")
		;;
esac
